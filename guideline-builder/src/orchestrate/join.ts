import { Constraint } from "./types";

export const join = (_constraint: Constraint): string => {
  /*
    Implementation
    - Should codegen DSL ts code from the given constraint tree 
  */
  return "dsl.always()";
};
